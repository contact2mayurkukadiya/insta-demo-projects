chrome.runtime.onMessage.addListener(function (request, sender, sendResponse) {
    // if (request.action == "loadLibrary") loadLibrary(sendResponse);
});

function loadLibrary(sendResponse) {
    // const script = document.createElement('script');
    // script.src = 'https://cdnjs.cloudflare.com/ajax/libs/jszip/3.6.0/jszip.min.js'; // Replace with the latest version from the CDN
    // script.type = 'text/javascript';
    // document.getElementsByTagName('head')[0].appendChild(script);
    // sendResponse({
    //     code: 200,
    //     action: "Library Loaded",
    // });
}